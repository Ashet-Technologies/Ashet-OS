hdoc(version="2.0", title="Custom Wallpapers");

p {
  To use a customized wallpaper, put an Ashet Bitmap in your desktop resolution at
  \mono{SYS:/etc/desktop/wallpaper.abm}.
  
  The desktop manager will then pick it up and show it as the background image.
}
