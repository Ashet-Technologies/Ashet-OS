Primary language is Zig.

The repo uses direct, low-level code with explicit types, small helper structs, snake_case naming, and concise doc comments where useful.

Prefer using the project-specified Zig binary (`zig-0.15.2` from `build.zig.zon` and `justfile`). Follow existing local patterns, avoid unrelated edits, and keep changes narrow to the requested area.
