When finishing a task, run the narrowest relevant verification command, preferably with `zig-0.15.2`.

For local libraries/tools this is often `zig-0.15.2 build test` in the package directory.

Report what was changed, what was verified, and any remaining failures or gaps if the underlying implementation is still incomplete.
