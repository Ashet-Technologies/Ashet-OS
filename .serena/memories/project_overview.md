Ashet OS is a tiny cross-platform operating system focused on hackability.

The repo is primarily Zig-based and includes OS/kernel code, ABI libraries, userland apps/libs, and tooling.

Top-level areas include `src/kernel`, `src/os`, `src/abi`, `src/userland`, `src/tools`, `assets`, `vendor`, and `docs`.